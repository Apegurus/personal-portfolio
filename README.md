# apeguru.dev
Static founder profile for Apeguru.
Edit page copy and links in `index.html`; keep protocol metrics attributed to their protocols.
Update `assets/images/og-card.html`, then render it at 1200×630 to regenerate `og-image.png`.
The favicon source and PNG fallback live in `assets/images/`.
